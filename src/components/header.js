import React, {Component} from 'react';
import {Text} from "@geist-ui/core";

class Header extends Component {
    render() {
        return (
            <Text h1> Sticky Notes! </Text>
        );
    }
}

export default Header;
